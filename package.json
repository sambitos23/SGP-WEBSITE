{
  "name": "sgp-website",
  "version": "2.0.0",
  "description": "Siliguri Government Polytechnic website with MongoDB Atlas backend deployed on Vercel",
  "private": true,
  "scripts": {
    "dev": "node dev-server.js",
    "vercel-dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "mongoose": "^8.5.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
